import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("👻")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
